class String
  define_method(:words_in_a_string) do |chosen_word|
    phrase = self
    phrase_words = phrase.split('')
    output = []
    phrase_words.each do |word|
      if chosen_word = word
        count()
    end
  end
   return count()
 end
 end
